const React = require('react');

const Logo = (props) => {
  return (
    <div id="logo">
      hello.
      <img src="../../public/TheSuccessFactory.jpg" alt="The Success Factory logo"></img>
    </div>
    );
};

module.exports = Logo;